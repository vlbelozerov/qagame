/**
 * Настройки конкурса. Правьте этот файл перед запуском — пересборка подхватит значения.
 *
 * ВАЖНО про пароль администратора:
 *  - когда задан SYNC_ENDPOINT, пароль НЕ хранится в этом бандле вообще: он уходит
 *    в Apps Script и сверяется там со Script Property ADMIN_PASSWORD;
 *  - ADMIN_PASSWORD_SHA256 нужен только для офлайн-режима (без endpoint), где проверять
 *    пароль больше негде. Любой хэш в статике подбирается перебором, поэтому в офлайне
 *    админка защищает от случайного захода, а не от целенаправленного взлома.
 */

export const config = {
  /** Название конкурса — выводится в шапке. */
  title: 'QA Game: Охота на баги',

  /**
   * Боевой режим входа.
   *
   * true  → стартовая страница с правилами: участник входит по имени, организатор —
   *         по логину и паролю. Это режим для реального конкурса.
   * false → демо-режим для показа коллегам: вход отключён, приложение сразу открывает
   *         тренажёр под гостевым логином, а в шапке есть кнопка перехода в админку
   *         без пароля.
   */
  requireLogin: true,

  /**
   * URL веб-приложения Google Apps Script (см. google-apps-script/README.md).
   * Пусто → приложение работает в офлайн-режиме с экспортом/импортом результатов.
   */
  syncEndpoint: 'https://script.google.com/macros/s/AKfycbxKs4qV0jqadUAohD-46UskgTI_sz9N7q5POOT3GAU3IsbxNrmdHW5PVuD8qur3LdY/exec',

  /** Логин администратора. */
  adminLogin: 'admin',

  /** SHA-256 от пароля админа. Используется ТОЛЬКО в офлайн-режиме. По умолчанию: "qa-admin-2026". */
  adminPasswordSha256: '5d5516d494ba6e3c5820066ca6c4cb500167f237bf3e22faafe3b381835e0d16',

  /**
   * Ограничение на корпоративный логин участника. Пустой массив → пускаем всех.
   * Пример: ['@company.ru'] — принимаем только логины с этим окончанием.
   */
  allowedLoginSuffixes: [] as string[],

  /** Минимальная длина логина участника. */
  minLoginLength: 3,

  /**
   * Длительность игры по умолчанию, минуты. Организатор может задать другую
   * при старте; 0 → игра без автозавершения, закрывается вручную.
   */
  gameMinutes: 60,

  /** Как часто проверять, есть ли что отправить на сервер, мс. */
  syncIntervalMs: 20_000,

  /**
   * Как часто отмечаться на сервере, когда отправлять нечего, мс.
   *
   * Обмен с находками уходит сразу, а вот пустой «я здесь» нужен только для того,
   * чтобы организатор видел, кто уже в игре. Каждый такой обмен берёт общую
   * блокировку скрипта, и если слать их часто, запуск игры встаёт в очередь за
   * ними и падает по таймауту блокировки.
   */
  heartbeatMs: 60_000,

  /**
   * Как часто участник спрашивает состояние игры.
   *
   * Пока игра не началась, витрина закрыта оверлеем, и задержка ответа — это фора
   * для тех, кто узнал о старте раньше. Поэтому в ожидании опрашиваем часто, во время
   * игры реже, а в скрытой вкладке почти не опрашиваем: каждый запрос тратит квоту
   * Apps Script. Если участников много и ожидание перед стартом долгое, увеличьте
   * roundPollWaitingMs — ценой нескольких секунд разброса на старте.
   */
  pollWaitingMs: 4_000,
  pollRunningMs: 15_000,
  pollHiddenMs: 60_000,
};

export type AppConfig = typeof config;
